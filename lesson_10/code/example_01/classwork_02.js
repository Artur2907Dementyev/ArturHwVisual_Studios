const countCharacters = (str) => {
    return str.length;
};

const countAndForget = (str) => {
    const length = str.length;
};

console.log(countCharacters('Hello World'));
countAndForget('Hello World');



const countSymbols = (text) => {
    const resultOfFunc = (typeof text === "string")
}